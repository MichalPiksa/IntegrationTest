# Python Integration Tests

Integration tests using **pytest + requests**.

## Requirements
- Python 3.10+
- Docker (optional, for CI)

## One-command setup
```bash
make install
```

## Secrets stored in "data/.env"